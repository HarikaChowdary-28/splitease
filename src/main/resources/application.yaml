spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/splitease
    username: splitease
    password: splitease
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

